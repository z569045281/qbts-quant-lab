name: Deploy dashboard to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - "frontend/**"
      - ".github/workflows/deploy-pages.yml"
  workflow_dispatch: {}

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install
        working-directory: frontend
        run: npm ci

      - name: Build static export
        working-directory: frontend
        env:
          # Project pages live under /<repo>/ — basePath must match.
          NEXT_PUBLIC_BASE_PATH: /${{ github.event.repository.name }}
          # Supabase read-only credentials (publishable key is safe to embed;
          # RLS restricts it to SELECT).
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY }}
          NEXT_PUBLIC_DEPLOY_READONLY: "1"
          # AWS Lambda publish Function URL — set as a repo Actions *variable*
          # (Settings → Secrets and variables → Actions → Variables). When set,
          # the "出今天的决策" button POSTs to Lambda; unset → button hidden.
          NEXT_PUBLIC_PUBLISH_URL: ${{ vars.NEXT_PUBLIC_PUBLISH_URL }}
        run: npm run build

      - uses: actions/upload-pages-artifact@v3
        with:
          path: frontend/out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
